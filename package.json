{
  "name": "lighthouse-pulse",
  "private": true,
  "scripts": {
    "cf:whoami": "wrangler whoami --env-file=.env",
    "cf:pages": "wrangler pages project list --env-file=.env",
    "cf:deploy": "wrangler pages deploy . --project-name=lighthouse-pulse --env-file=.env"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
